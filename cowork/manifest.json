{
  "manifest_version": "0.3",
  "name": "muster",
  "display_name": "Muster",
  "version": "0.3.1",
  "description": "Glass-box agentic router. Exposes muster's deterministic CLI brain (capability and domain routing, gate scoring, RICE prioritization, wave planning) as MCP tools.",
  "long_description": "Muster's deterministic core (project detection, role-to-provider resolution, artifact gate scoring, dependency-ordered wave planning) wrapped as a local MCP server for Claude Cowork. The router runs as plain Node and makes no model calls; Claude calls these tools to decide who does the work, then does the judgment work itself. Principles, routing policy, and an execution protocol are injected via the server's MCP instructions. Note: the server spawns the muster CLI as a child process, which can be blocked on Windows MSIX installs (virtualized bundle path); verify with scripts/cowork-probe.mjs before relying on it there.",
  "author": { "name": "rnbennett" },
  "license": "MIT",
  "server": {
    "type": "node",
    "entry_point": "cowork/mcp-server.mjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/cowork/mcp-server.mjs"],
      "env": {
        "MUSTER_ENABLE_FABLE": "${user_config.enable_fable}",
        "MUSTER_MAX_TIER": "${user_config.max_tier}",
        "MUSTER_COWORK_CONNECTORS": "${user_config.connectors}"
      }
    }
  },
  "user_config": {
    "enable_fable": {
      "type": "boolean",
      "title": "Enable Fable tier",
      "description": "Route peak-judgment roles to Fable. Off by default; degrades to Opus when disabled.",
      "default": false
    },
    "max_tier": {
      "type": "string",
      "title": "Max model tier",
      "description": "Cap dispatch at 'opus' or 'sonnet' for budget control. Leave empty for no cap.",
      "default": ""
    },
    "connectors": {
      "type": "string",
      "title": "Declared remote connectors",
      "description": "Comma-separated names of remote connectors to treat as available. Remote connectors cannot be auto-discovered from disk.",
      "default": ""
    }
  },
  "tools": [
    { "name": "muster_detect", "description": "Detect the project profile for a directory." },
    { "name": "muster_capabilities", "description": "Resolve every role to its best-available provider, chain, and model tier." },
    { "name": "muster_match", "description": "Rank catalog providers against a free-text task." },
    { "name": "muster_domain", "description": "Classify an outcome into a work domain." },
    { "name": "muster_route", "description": "Route an outcome to its domain + pipeline." },
    { "name": "muster_pipeline", "description": "Load a pipeline definition by domain or id." },
    { "name": "muster_assess", "description": "Gap-check an outcome before running." },
    { "name": "muster_steer", "description": "Classify a mid-run steer message." },
    { "name": "muster_diagnose", "description": "Classify a failure symptom and build a diagnose manifest." },
    { "name": "muster_audit", "description": "Build the whole-codebase audit manifest." },
    { "name": "muster_manifest_validate", "description": "Validate a crew manifest's shape and dependency graph." },
    { "name": "muster_wave", "description": "Compute dependency-ordered execution waves." },
    { "name": "muster_next", "description": "Single-agent driver: next runnable task given completed ids." },
    { "name": "muster_score", "description": "Score an artifact against a gate." },
    { "name": "muster_prioritize", "description": "Rank backlog items by RICE/ICE/WSJF/weighted." },
    { "name": "muster_pick", "description": "Pick the tournament winner from scored candidates." },
    { "name": "muster_tally", "description": "Tally adversarial review verdicts into a gate decision." }
  ],
  "compatibility": {
    "runtimes": { "node": ">=20" }
  }
}
