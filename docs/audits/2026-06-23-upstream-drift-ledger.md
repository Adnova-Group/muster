# Upstream Drift Ledger — skill/agent updates worth adopting

_Run: `muster/upstream-drift-audit` · base `main` · generated by autopilot_

## Premise correction

The outcome named "atomic-claude, superpowers, gsd". Muster vendors **three** MIT sources per `vendor/manifest.yaml` + `NOTICE` — **atomic-claude is not one of them**:

| Source | Kind | Pin | Upstream HEAD | Drift |
|---|---|---|---|---|
| `obra/superpowers` | local (no SHA) | — | `896224c` | sp-subagents +132L (5 new sections), sp-plan +2 sections; rest ±noise |
| `wshobson/agents` | github | `cf6059d` | `cc37bfd` | +5 commits / 13 files — **none vendored** |
| `open-gsd/gsd-core` | github | `85cfa5d` | `c28cccb` | +462 commits; all 3 vendored workflows changed, but gsd-internal |

**Clean-room (no upstream, intentional divergence — do not auto-align):** skills `router, orchestrator, interview, tournament, review-gate, domain-router, greenfield, prd-pipeline, roadmap-prioritization`; agents `muster-{builder,investigator,reviewer,strategist,surgeon}`.

## Ranked findings (impact × effort × fit)

### 1. ADAPT — sp-subagents "explicit model per dispatch" · impact HIGH · effort LOW · fit EXACT
Upstream added: _"Always specify the model explicitly when dispatching a subagent. An omitted model inherits your session's model — often the most capable and most expensive."_ + "architecture/review tasks scale model to risk" + "turn count beats token price."
→ This **validates** muster's existing rule (manifest requires `model` on every non-inline crew member; orchestrator dispatches on the role's resolved model). Adopt the rationale prose into the `orchestrator`/`router` skill docs as the canonical "why". Target: `plugin/skills/orchestrator/SKILL.md`.

### 2. ADAPT — sp-subagents "Pre-Flight Plan Review" · impact HIGH · effort MED · fit STRONG
Upstream added a step: before dispatching Task 1, scan the plan once for conflicts (contradictory tasks, plan mandates that the review rubric would flag as defects), batch them to the human as **one** question, then proceed.
→ Muster's orchestrator dispatches waves straight from the router's plan with no pre-flight conflict scan. Adding a batched plan-conflict gate before wave 1 hardens `/muster:autopilot`. Target: `plugin/skills/orchestrator/SKILL.md` (new pre-wave step).

### 3. ADAPT — sp-plan "Task Right-Sizing" + "Global Constraints" · impact MED · effort LOW · fit STRONG
Upstream added explicit task-right-sizing heuristics and a global-constraints section threaded through tasks.
→ Muster's `router` decomposes outcomes into `plan[]` tasks but gives no sizing/constraints guidance. Port the heuristics into the router skill so manifests carry right-sized tasks + shared constraints. Target: `plugin/skills/router/SKILL.md`.

### 4. ADAPT — sp-subagents "Durable Progress" + "File Handoffs" + "Handling ⚠️ Items" · impact MED · effort LOW–MED · fit PARTIAL
Upstream formalized a progress ledger, diff-file handoff between implementer↔reviewer subagents, and reviewer-warning triage.
→ Muster already has STATE glass-box + the review-gate. Lift the diff-file handoff + ⚠️-triage specifics into `plugin/skills/review-gate/SKILL.md`; the progress-ledger idea is largely already covered by STATE.

### 5. REFRESH (process) — pin superpowers in vendor/manifest.yaml · impact LOW · effort LOW · fit EXACT
`superpowers` is `kind: local` with no `ref:`, so `muster vendor` cannot detect upstream drift the way it does for wshobson/gsd. Pin it to a real SHA (`896224c` or current) and switch to `kind: github` so future superpowers drift is reviewable. Target: `vendor/manifest.yaml`.

### 6. SKIP — gsd workflow churn (462 commits)
`plan-phase +158/-237`, `execute-phase +105/-160`, `verify-work +58/-13` are real but gsd-internal: `RUNTIME_DIR` resolution, AI-SPEC artifact resolution, UI Design Contract Gate, `uat classify-coverage` coverage classification, schema-drift gates. They depend on gsd-core's own CLI/runtime that muster does not import. Not portable without dragging in gsd machinery.
→ One idea worth a later look (not now): gsd's verify-work **coverage-aware deterministic classification** (auto-pass automated coverage, only prompt the human on prose) could inspire muster's `verify`/review surface. Logged as adapt-later, not a port.

### 7. SKIP — wshobson drift
The 5 upstream commits touch repo docs, `plugin-eval` tooling, and one non-vendored skill (`prompt-engineering-patterns`). **No vendored agent or skill changed.** Re-pin only when adopting new agents.

## Net recommendation
Four low/medium-effort superpowers adaptations (#1–#4) flow upstream thinking into muster's clean-room orchestrator/router/review-gate; one process fix (#5) makes superpowers drift visible going forward. gsd and wshobson need no action this cycle.
