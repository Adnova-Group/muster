TASK: fix-flaky-retry-test
ROLE: debug

Diagnose and fix the flaky retry test in the payments service. This wave has a
single task, so there is nothing else in flight to fence against.

## Return contract
Return raw data, <=2000 chars: files changed (as paths), test counts, deviations
one line each. No code snippets, stack traces, or file dumps.
