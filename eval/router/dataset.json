{
  "prompt": "plugin/skills/router/SKILL.md",
  "description": "Empirical eval of the router skill: outcome -> Crew Manifest JSON. Each case is graded by code (manifest validates + crew non-empty) and by an LLM judge against a per-case rubric. passThreshold 7/10.",
  "passThreshold": 7,
  "cases": [
    {
      "id": "auth-backend",
      "task": "Add JWT authentication to the Express REST API, including tests.",
      "expectRoles": ["implement", "test-author"],
      "rubric": "A good manifest routes this software task to an implementation provider plus a test author; a security-review dimension is a bonus. Crew providers should be real (not all inline). The plan should build the endpoint then test it. Penalize: content/author roles, missing tests, empty/all-inline crew."
    },
    {
      "id": "security-audit",
      "task": "Audit the entire codebase for security vulnerabilities and remediate the findings.",
      "expectRoles": ["security-review", "code-review"],
      "rubric": "A good manifest fans out review dimensions including a security-review provider, consolidates findings, then fixes with a verify gate. Penalize: a single implement-only crew, no security dimension, no remediation/verify step."
    },
    {
      "id": "blog-post",
      "task": "Write a 600-word launch blog post announcing our new analytics dashboard.",
      "expectRoles": ["author", "humanize"],
      "rubric": "A good manifest routes this content task to an authoring provider and ends with a humanize phase (strip AI-tells). Penalize: software/implement roles, code review, missing humanize."
    },
    {
      "id": "fix-failing-test",
      "task": "Diagnose and fix the failing unit test in the payments service.",
      "expectRoles": ["debug", "implement"],
      "rubric": "A good manifest is failure-first: reproduce/diagnose the failure before fixing, then verify the suite is green. Penalize: jumping to a broad feature build, no reproduction step, no verify."
    },
    {
      "id": "refactor-utils",
      "task": "Refactor the legacy dateUtils module for readability without changing behavior.",
      "expectRoles": ["refactor"],
      "rubric": "A good manifest routes to a refactor/simplification provider and includes a behavior-preservation check (tests stay green). Penalize: feature additions, behavior changes, no regression guard."
    },
    {
      "id": "research-vectordb",
      "task": "Research and recommend a vector database for our RAG pipeline.",
      "expectRoles": ["research", "docs-research"],
      "rubric": "A good manifest routes to a research provider that gathers and synthesizes evidence and ends with a cited recommendation. Penalize: jumping to implementation, no evidence/sources, code-heavy crew."
    },
    {
      "id": "luca-finance-poc",
      "task": "Build a proof-of-concept AI finance chat app over QuickBooks for a prospective customer demo: a chat UI over live QuickBooks data, a branded monthly report, and a deterministic forecast.",
      "expectRoles": ["implement", "frontend", "humanize"],
      "rubric": "A good manifest is multi-surface: a schema/migration task bound to the supabase skill, a chat-UI task bound to nextjs/shadcn plus a design skill and tagged surface ui, a branded-report task bound to the humanizer and tagged surface copy, and a QBO OAuth task bound to sp-verify and tagged surface integration. Every binding carries distinct rationale (why this task) and evidence (the quoted signal). Penalize: any skills[] that is merely non-empty without matching these ids, missing surface tags, an unflagged ai-sdk gap (the fixture's inventory deliberately omits it), or bindings that reuse one field as both rationale and evidence.",
      "note": "Regression case (t7): the original failure was a Next.js+Supabase customer-facing app built without supabase/vercel/design/humanizer/verification skills ever firing. This case's golden manifest is the guard: strip its plan[].skills and this case's dedicated skill-binding assertions in test/router-eval.test.js must fail.",
      "profile": { "frameworks": ["nextjs", "supabase"], "languages": ["typescript"], "context": "customer-facing" },
      "skillsInventory": [
        { "id": "nextjs", "source": "installed", "description": "Next.js App Router routing/rendering patterns." },
        { "id": "shadcn", "source": "installed", "description": "shadcn/ui component library for React." },
        { "id": "supabase", "source": "installed", "description": "Supabase schema, auth, and RLS patterns." },
        { "id": "frontend-design", "source": "installed", "description": "Dedicated frontend design pass for user-facing UI." },
        { "id": "muster-humanizer", "source": "builtin", "description": "Strip AI-tell phrasing from customer-facing copy." },
        { "id": "sp-verify", "source": "builtin", "description": "Verification-before-completion for integration claims." }
      ],
      "expectedSkillGap": "ai-sdk"
    }
  ]
}
