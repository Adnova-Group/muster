{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "muster": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/mcp/agent-plugins-server.mjs"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
